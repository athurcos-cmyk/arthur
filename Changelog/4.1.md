# CHANGELOG

## [4.1.0] - Refinamento UX & Segurança - 23/11/2025

### 🛡️ Segurança & Estabilidade
- **Correção de "Timer Congelado":**
  - Refatorada a função `renderQuestion` no `script.js`. Agora o timer (`startQuizTimer`) é iniciado estritamente **após** a renderização do HTML, corrigindo o bug onde o relógio travava ao avançar perguntas.
- **Proteção de Loop:**
  - Ajustada a função `switchTab` para limpar intervalos de timer antigos antes de iniciar novos, prevenindo múltiplos relógios rodando em segundo plano.

### 🎨 Experiência do Usuário (UX)
- **Notificações Modernas:**
  - Substituído o `alert()` nativo (bloqueante/intrusivo) do Pomodoro pelo sistema de **Toast** (`showToast`). Agora o aviso de "Tempo Esgotado" é suave e não trava o navegador.
- **Feedback Sonoro:**
  - Adicionado efeito de áudio (beep) ao finalizar o ciclo Pomodoro (funcionalidade online).
- **Onboarding (Guia do Sistema):**
  - Implementado botão `?` na navbar.
  - Criado Modal informativo explicativo sobre as funcionalidades (Quiz, Timer, Tracker).
  - Adicionado rodapé com versão do sistema no modal.

### 🧠 Quiz
- **Developer Experience (DX):**
  - Adicionado display automático do **ID do Tópico** (ex: `ID: 0-2-5`) na tela de "Quiz não encontrado". Isso facilita a cópia do ID correto para cadastro de novas questões no `quiz.js`.

---

## [4.0.0] - Sistema de Quiz & Gamificação - 23/11/2025
*(Funcionalidades anteriores mantidas)*
- Aba Quiz com Timer e Embaralhamento Duplo.
- Arquitetura separada (`quiz.js`).